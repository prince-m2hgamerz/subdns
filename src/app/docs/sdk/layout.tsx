import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "SDK Reference — SubDNS",
  description:
    "Integrate SubDNS into your applications with our JavaScript/TypeScript SDK. Create and manage subdomains programmatically.",
};

export default function SdkDocsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
